name: bubuy_lovers
description: Aplikasi berita hewan Bubuy Lovers

publish_to: 'none' # Remove this line if you intend to publish your package

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Assets configuration - pastikan logo bubuy.png ter-load
  assets:
    - assets/images/ # This line includes all files in the assets/images/ directory